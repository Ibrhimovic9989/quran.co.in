{
  "name": "@quran/database",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Prisma schema + generated client for quran.co.in (web + api)",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "dotenv -e ../../apps/web/.env.local -- prisma migrate dev",
    "studio": "dotenv -e ../../apps/web/.env.local -- prisma studio",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "prisma": "^6.1.0"
  }
}
