# Base URL for the quran.co.in NestJS backend (apps/api).
# Local dev points at the deployed API by default; set to http://localhost:3001
# if you are running the backend locally.
NEXT_PUBLIC_API_URL=https://api.quran.co.in
