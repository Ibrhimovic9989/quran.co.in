# quran.co.in API — environment
# Copy to .env for local development.

# Server
PORT=3001
NODE_ENV=development

# CORS: comma-separated allowed web origins
WEB_ORIGIN=http://localhost:3000

# Database (Supabase Postgres with pgvector)
DATABASE_URL=postgresql://user:password@host:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://user:password@host:5432/postgres

# External Quran API override (optional)
# QURAN_COM_API_URL=

# --- Phase 2 (auth) ---
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# JWT_ACCESS_SECRET=
# JWT_REFRESH_SECRET=
# COOKIE_DOMAIN=.quran.co.in

# --- AI: free-tier fallback chain (tried in order; set whichever you have) ---
# 1) NVIDIA NIM — free at https://build.nvidia.com (key looks like nvapi-...)
#    Rate limits are PER MODEL, so every model in the list adds free capacity.
#    Defaults to a curated 10-model llama/nemotron/qwen/mixtral chain; override
#    with a comma-separated list (any of the free build.nvidia.com models).
# NVIDIA_API_KEY=
# NVIDIA_CHAT_MODELS=meta/llama-3.3-70b-instruct,meta/llama-3.1-8b-instruct
#
# 2) OpenRouter — free models at https://openrouter.ai (key looks like sk-or-...)
#    Free quota is account-wide; defaults to a curated :free model list.
#    Later: load credit and set OPENROUTER_CHAT_MODELS to a paid model first.
# OPENROUTER_API_KEY=
# OPENROUTER_CHAT_MODELS=meta-llama/llama-3.3-70b-instruct:free,deepseek/deepseek-chat:free
#
# 3) Azure OpenAI (legacy, optional last-resort for chat; REQUIRED for vector
#    semantic search until the corpus is re-embedded with a free model —
#    the pgvector corpus is text-embedding-3-small/1536-dim).
#    Without it, search degrades to Postgres full-text automatically.
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_ENDPOINT=
# AZURE_OPENAI_EMBEDDING_DEPLOYMENT=
# AZURE_OPENAI_CHAT_DEPLOYMENT=
# AZURE_OPENAI_API_VERSION=
# AZURE_OPENAI_CHAT_API_VERSION=
